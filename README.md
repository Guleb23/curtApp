# ⚖️ CurtService

> 🧩 Микросервисная система ведения судебных дел — автоматизация, уведомления и контроль сроков в одном месте.

---

## 🏗️ Архитектура проекта

CurtService состоит из **четырёх микросервисов**, взаимодействующих через **REST API**  
и объединённых с помощью **Docker Compose**.

```mermaid
graph TD
    A[Frontend: React] -->|"REST API"| B[Backend: ASP.NET Core]
    B -->|"PostgreSQL"| C[(Database)]
    D[Background Service] -->|"Проверка дел"| B
    D -->|"Отправка уведомлений"| E[Notification Service]
    E -->|"SMTP"| F[(Email Server)]
