# ⚖️ CurtService

> 🧩 Микросервисная система ведения судебных дел — автоматизация, уведомления и контроль сроков в одном месте.

---

## 🏗️ Архитектура проекта

CurtService состоит из **четырёх микросервисов**, взаимодействующих через **REST API**  
и объединённых с помощью **Docker Compose**.

```mermaid
graph TD
    A[Frontend: React] -->|"REST API"| B[Backend: ASP.NET Core]
    B -->|"PostgreSQL"| C[(Database)]
    D[Background Service] -->|"Запрос к Notification Service"| E[Notification Service]
    D -->|"Прямое подключение"| C
    E -->|"SMTP"| F[(Email Server)]


1. ### 🖥️ Frontend (React)
   - Разработан на **React + Js**
   - Авторизация через **JWT (Access + Refresh Tokens)**
   - Поддержка всех CRUD-операций по работе с судебными делами и инстанциями
   - Взаимодействует с backend через REST API

2. ### ⚙️ Backend API (ASP.NET Core)
   - Реализует бизнес-логику системы
   - Поддерживает **JWT-аутентификацию и авторизацию**
   - CRUD-операции для:
     - Судебных дел  
     - Судебных инстанций  
     - Пользователей и ролей
   - Работает с базой данных **PostgreSQL**
   - Предоставляет REST API для других сервисов

3. ### ⏰ Background Service
   - Фоновый микросервис для автоматической проверки просроченных дел
   - Выполняется **ежедневно в 6:00 утра**
   - При обнаружении просроченных дел — отправляет запросы в Email Service

4. ### 📧 Email Service
   - Микросервис для **отправки уведомлений по email**
   - Получает запросы от Background Service и использует SMTP / Mailkit API

---

## 🧩 Технологический стек

| Компонент | Технология |
|------------|-------------|
| **Frontend** | React, js |
| **Backend** | ASP.NET Core |
| **Auth** | JWT (Access + Refresh Tokens) |
| **Database** | PostgreSQL |
| **Architecture** | Микросервисная |
| **Communication** | REST API |
| **Infrastructure** | Docker, Docker Compose |
| **Background Jobs** | Cron (через .NET Hosted Service / Timer) |
| **Email Service** | SMTP / Mailkit API |

---

## 🚀 Запуск проекта

```bash
# Сборка и запуск всех микросервисов
docker-compose up --build
